# nginx/default.conf
server {
    listen 80;
    server_name localhost;
    root /var/www/html/public; # Laravel via "public/"

    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
}